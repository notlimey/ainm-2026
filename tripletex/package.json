{
  "name": "tripletex-agent",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "ai": "^4",
    "@ai-sdk/google": "^1",
    "hono": "^4",
    "zod": "^3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "vitest": "^3",
    "wrangler": "^4",
    "typescript": "^5"
  }
}
